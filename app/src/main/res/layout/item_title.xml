<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <data >

        <variable
            name="gnomo"
            type="com.tonacastro.gnomos.domain.gnomos.model.GnomoModel" />

        <variable
            name="mainViewModel"
            type="com.tonacastro.gnomos.viewmodel.detail.DetailViewModel" />

        <variable
            name="gnomosAdapter"
            type="com.tonacastro.gnomos.view.adapters.GenericListAdapter" />

    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:orientation="vertical"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/activity_vertical_margin"
        android:background="@drawable/btn_border_outline">
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:orientation="horizontal">
            <TextView
                android:id="@+id/item_tittle_tvtittle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/activity_vertical_margin"
                style="@style/titleBlueText"
                tools:text="Tonatiuh Castro"/>
            <TextView
                android:id="@+id/item_tittle_tvsum"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/activity_vertical_margin"
                style="@style/titleBlueText"
                android:visibility="gone"
                tools:text="(45)"/>
        </LinearLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>